import {readdir,readFile,writeFile,copyFile} from 'node:fs/promises';
import {createHash} from 'node:crypto';
async function walk(dir){const result=[];for(const entry of await readdir(dir,{withFileTypes:true})){const path=`${dir}/${entry.name}`;if(entry.isDirectory())result.push(...await walk(path));else if(entry.name!=='sw.js')result.push(path);}return result;}
const paths=(await walk('dist')).sort();
const hash=createHash('sha256');for(const path of paths)hash.update(await readFile(path));
const version=hash.digest('hex').slice(0,16),assets=paths.map(p=>p.slice(5));
await writeFile('dist/sw.js',`const PREFIX='eksaar-panchang-'+btoa(self.registration.scope)+'-';\nconst CACHE=PREFIX+'${version}';\nconst ASSETS=${JSON.stringify(assets)};\nself.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(ASSETS.map(p=>new URL(p,self.registration.scope).href)))));\nself.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith(PREFIX)&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));\nself.addEventListener('fetch',event=>{if(event.request.method!=='GET'||new URL(event.request.url).origin!==self.location.origin)return;event.respondWith(caches.open(CACHE).then(async cache=>{if(event.request.mode==='navigate'){return (await cache.match(new URL('index.html',self.registration.scope).href,{ignoreVary:true}))||fetch(event.request);}return (await cache.match(event.request,{ignoreVary:true}))||fetch(event.request);}));});\n`);
console.log(`Offline cache ${version}: ${assets.length} bundled files.`);
